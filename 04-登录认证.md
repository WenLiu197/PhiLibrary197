Phigros 云存档使用 **TapTap** 账号体系（后端为 LeanCloud）。登录流程：

```
TapTap 登录 ──> LCCombinedAuthData ──> LCHelper.LoginAndGetToken ──> session token ──> new Save(token)
```

## 方式一：QRCode 扫码登录

```csharp
using PhiLibrary197.CloudSave.Login;

// 1. 请求二维码数据
CompleteQRCodeData qr = await TapTapHelper.RequestLoginQrCode(useChinaEndpoint: false);

// 2. 展示二维码（qr.Url），等待用户扫码
Console.WriteLine($"扫码: {qr.Url}");

// 3. 轮询确认（间隔数秒）
TapTapTokenData? tokenData = null;
while (tokenData is null)
{
    await Task.Delay(TimeSpan.FromSeconds(3));
    tokenData = await TapTapHelper.CheckQRCodeResult(qr, useChinaEndpoint: false);
}

// 4. 拉取用户资料并组装登录数据
TapTapProfileData profile = await TapTapHelper.GetProfile(tokenData.Data, useChinaEndpoint: false);
LCCombinedAuthData auth = new(profile.Data, tokenData.Data);

// 5. 换取 session token
string sessionToken = await LCHelper.LoginAndGetToken(auth, useChinaEndpoint: false);
```

## 方式二：回调 URL 登录（OAuth 2.0）

适合本机应用：生成授权链接 → 用户浏览器授权 → 回调换 token。

```csharp
// 1. 生成授权链接（redirect_uri 指向本地监听服务）
CallbackLoginData login = TapTapHelper.GenerateCallbackLoginUrl(
    callbackUrl: "http://localhost:51747/callback",
    useChinaEndpoint: false);

Console.WriteLine($"请浏览器打开: {login.BeginUrl}");
// 保存 login.CodeVerifier，换 token 时使用

// 2. 回调端点收到 ?code=xxx 后：
string code = "...";
TapTapTokenData tokenData = await TapTapHelper.HandleCallbackLogin(login, code, useChinaEndpoint: false);

// 3. 后续同扫码流程（拉 profile → 组装 → 换 token）
TapTapProfileData profile = await TapTapHelper.GetProfile(tokenData.Data, useChinaEndpoint: false);
LCCombinedAuthData auth = new(profile.Data, tokenData.Data);
string sessionToken = await LCHelper.LoginAndGetToken(auth, useChinaEndpoint: false);
```

> [!NOTE]
> 回调登录仅支持 localhost 回调，不适合 Discord bot 等服务器场景（上游限制）。

## 方式三：直接组装（已有 TapTap 凭证）

```csharp
TapTapProfileData.ProfileData pd = new() { OpenId = "...", UnionId = "...", Name = "...", Avatar = "..." };
TapTapTokenData.TokenData td = new() { Kid = "...", Token = "...", TokenType = "...", MacKey = "...", MacAlgorithm = "...", Scope = "public_profile" };
LCCombinedAuthData auth = new(pd, td);
string sessionToken = await LCHelper.LoginAndGetToken(auth, useChinaEndpoint: false);
```

## 端点选择

所有方法都有 `useChinaEndpoint` 参数：

| 值 | 说明 |
| --- | --- |
| `false`（默认） | 国服：TapTap 中国域名 + LeanCloud 国服 |
| `true` | 国际服：TapTap 国际域名 + LeanCloud 国际服 |

`TapTapHelper` 暴露全部端点常量：`GetWebHost(bool)`、`GetCodeUrl(bool)`、`GetTokenUrl(bool)`、`GetProfileUrl(bool, bool)`、`GetAccountUrl(bool)` 等。

## 类型速览

| 类型 | 用途 |
| --- | --- |
| `CompleteQRCodeData` | 扫码登录：`Url`、`DeviceCode`、`DeviceID`、`ExpiresInSeconds`、`Interval` |
| `TapTapTokenData` | `Data.TokenData`：`Kid`、`Token`、`TokenType`、`MacKey`、`MacAlgorithm`、`Scope` |
| `TapTapProfileData` | `Data.ProfileData`：`OpenId`、`UnionId`、`Name`、`Gender`、`Avatar` |
| `CallbackLoginData` | 回调登录：`BeginUrl`、`CodeVerifier`、`RedirectUrl`、`State`、`CodeChallenge`、`Scope` |
| `LCCombinedAuthData` | 组合数据：`Kid`、`Token`、`MacKey`、`MacAlgorithm`、`OpenID`、`Name`、`Avatar`、`UnionID` |

`LoginAndGetToken` 等价于 `LoginWithAuthData(...)["sessionToken"]`。
