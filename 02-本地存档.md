Phigros 本地存档位于 `playerPrefsV2.xml`，成绩以 `键 = 密文值` 的键值对存储。

## 解析流程

1. 从 `playerPrefsV2.xml` 读取键值对字符串
2. URL 解码（`WebUtility.UrlDecode`）得到 Base64
3. `LocalSave.DecryptLocalSaveStringNew` 解密得到明文字符串
4. 成绩值解析为 `RawScore`（JSON），键解析出歌曲 id 与难度

## 解密

```csharp
using PhiLibrary197.LocalSave;
using System.Net;

string rawKey   = "Cgw4SttKwRFIjb68TF8z5EC%2FLwVpK8KjKmjcm9T3M78%3D";
string rawValue = "eSB6QJlXU1vwHjVx7kcpb4%2Fjdk9o5j4Wiatn%2BjrJ%2BetI2KFMlPDyH8s7I8zM%2BqlW";

string key   = LocalSave.DecryptLocalSaveStringNew(WebUtility.UrlDecode(rawKey));
string value = LocalSave.DecryptLocalSaveStringNew(WebUtility.UrlDecode(rawValue));
```

| 重载 | 说明 |
| --- | --- |
| `DecryptLocalSaveStringNew(string base64EncryptedString)` | 输入 Base64 字符串 |
| `DecryptLocalSaveStringNew(byte[] decodedCipherText)` | 输入解码后的字节 |

## 解析成绩

解密后的值形如 `{"s":992580,"a":99.17559051513672,"c":1}`：

```csharp
RawScore raw = RawScore.FromJson(value);
Console.WriteLine($"score={raw.Score}, acc={raw.Accuracy}, status={raw.Status}");
```

`RawScore` 字段（JSON 键名）：

| 属性 | JSON 键 | 说明 |
| --- | --- | --- |
| `Score` | `s` | 分数（0 ~ 1,000,000） |
| `Accuracy` | `a` | 准确率（0 ~ 100） |
| `Status` | `c` | 原始状态（0 = 非 FC，1 = FC…） |

解密后的键形如 `MARENOL.LeaF.0.Record.HD`：前 3 段是歌曲 id（`MARENOL.LeaF.0`），末段是难度（`HD`）。

## 组装 SongScore

```csharp
using PhiLibrary197.CloudSave;

string[] parts = key.Split('.');
string songId  = string.Join(".", parts.Take(3));
Difficulty diff = Enum.Parse<Difficulty>(parts[^1]);

SongScore song = raw.ToSongScore(songId, diff);
Console.WriteLine($"{song.Id} {song.Difficulty}: {song.Score} pts, {song.Status}");
```

## 完整示例

```csharp
using System.Net;
using System.Xml.Linq;
using PhiLibrary197.CloudSave;
using PhiLibrary197.LocalSave;

XDocument doc = XDocument.Load("playerPrefsV2.xml");
var map = doc.Descendants("string").ToDictionary(
    e => e.Attribute("name")!.Value,
    e => e.Value);

List<SongScore> scores = [];
foreach (var (k, v) in map)
{
    string decKey = LocalSave.DecryptLocalSaveStringNew(WebUtility.UrlDecode(k));
    if (!decKey.Contains(".Record.")) continue; // 只取成绩项

    string[] parts = decKey.Split('.');
    string id = string.Join(".", parts.Take(3));
    Difficulty diff = Enum.Parse<Difficulty>(parts[^1]);
    scores.Add(RawScore.FromJson(
        LocalSave.DecryptLocalSaveStringNew(WebUtility.UrlDecode(v)))
        .ToSongScore(id, diff));
}
```

> [!NOTE]
> `DecryptLocalSaveStringNew` 是本库（v5 系）的方法名，上游更早版本为 `DecryptSaveString`，以本库实际 API 为准。
