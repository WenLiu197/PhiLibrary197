## 许可证

| 文件 | 内容 |
| --- | --- |
| `LICENSE` | **GPL-3.0**（本项目整体许可，含修改部分） |
| `LICENSE.MIT` | **MIT**（上游原许可全文，保留原作者 yt6983138 / static_void 版权声明） |

NuGet 包内同时包含这两个文件，满足：

- GPLv3 §4：许可文本随副本分发
- MIT：版权声明与许可声明随副本传播

> [!WARNING]
> 许可证按合同执行。任何分发/再许可/修改前请核对 GPLv3 与 MIT 义务。

## 署名

- 原代码版权：`Copyright © 2026 yt6983138 aka static_void`（保留于包元数据）
- 修改部分版权：WenLiu197
- 包元数据：`Authors = WenLiu197`，包描述声明派生关系

## 上游跟踪

```bash
git remote add upstream git@github.com:yt6983138/PhigrosLibraryCSharp.git
git fetch upstream
git diff master upstream/v5  # 评估上游更新后按需合入
```

> [!NOTE]
> 上游作者说明未来只更新 v5 分支，基于 v4 的 fork 不会获得更新。本库基于 v5。

## NuGet 发布

发布走 **Trusted Publishing**（GitHub OIDC，无长期 API 密钥）：

1. nuget.org → 受信任的发布 → 策略：`WenLiu197 / PhiLibrary197 / publish.yml`
2. GitHub Secrets：`NUGET_USER` = nuget.org 用户名
3. 打 tag 触发发布：

```bash
git tag v0.2.0
git push origin v0.2.0
```

workflow（`.github/workflows/publish.yml`）构建 slnx → `NuGet/login@v1` 用 OIDC 换临时密钥 → `dotnet nuget push`。

## 文档站发布

`.github/workflows/retype-action.yml`：master 推送或手动触发，构建 `doc/` 并部署到 `gh-pages` 分支。

## 分支工作流

- 每次新操作开分支（`git switch -c <分支>`）
- 完成后本地合并 master 再推送（不用 PR）：

```bash
git switch master
git merge <分支>
git push origin master
```
