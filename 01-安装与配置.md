## 安装

包 ID 为 `PhiLibrary197`，可通过以下任一方式引用：

```bash
# .NET CLI
dotnet add package PhiLibrary197
```

```powershell
# Package Manager
Install-Package PhiLibrary197
```

或手动编辑项目文件：

```xml
<ItemGroup>
  <PackageReference Include="PhiLibrary197" Version="0.2.0" />
</ItemGroup>
```

> [!NOTE]
> 包 ID 是 `PhiLibrary197`，不是上游的 `PhigrosLibraryCSharp`。命名空间同样为 `PhiLibrary197.*`（如 `PhiLibrary197.CloudSave`）。

## 可选配置项

库零配置即可使用，以下均为可选定制项。

### 共享 HttpClient

所有 `Save` 实例默认共享一个全局 `HttpClient`（连接生命周期 2 分钟）。需要自定义时替换：

```csharp
Save.SharedClient = new HttpClient(new SocketsHttpHandler
{
    Proxy = new WebProxy("http://127.0.0.1:7890"),
    UseProxy = true,
});
```

### 请求拦截（按实例）

```csharp
Save save = new("token", false);
save.RequestHandler = async (s, request, ct) =>
{
    request.Headers.Add("X-Custom", "1");
    return await s.Client.SendAsync(request, ct);
};
```

### 自定义 AES 实现

默认使用 `Aes`（PKCS7 填充）。在 WASM 等无 AES 的环境必须替换：

```csharp
save.Decryptor = async (key, iv, data, ct) => /* 你的解密实现 */;
save.Encryptor = async (key, iv, data, ct) => /* 你的加密实现 */;
```

### 自定义 MD5（登录签名）

```csharp
LCHelper.GetMD5HashHexString = async (input, ct) => MyMd5(input);
```

### TapTap 请求代理

```csharp
TapTapHelper.Proxy = (client, request) => client.SendAsync(request, CancellationToken.None);
```

## Native AOT 支持

库已标记 `IsAotCompatible`，全部 JSON 序列化走源生成器。应用要 AOT 发布时：

```xml
<PropertyGroup>
  <PublishAot>true</PublishAot>
</PropertyGroup>
```

```bash
dotnet publish -c Release -r win-x64 --self-contained -p:PublishAot=true
```

序列化细节见 [序列化与 AOT](08-序列化与AOT.md)。
